// app.js

import './bootstrap'; // default Laravel Vite setup (if you're using Laravel's default bootstrap.js)

import 'jquery'; // Ensure jQuery is installed through npm
import 'bootstrap'; // Bootstrap (make sure it's installed via npm)

import 'select2';
import 'daterangepicker';
import 'countdowntime';
import './main'; // Assuming 'main.js' is the custom JS you referenced before
